---
context: phase_boundary
phase: P3 (done — UAT paused 1/20) → P4 (ready)
task: 1
total_tasks: 20
status: paused_during_uat
last_updated: 2026-04-18T02:54:51+11:00
session: 4 (Koda)
---

# Atlas AI — P3 code shipped; UAT paused at test 1/20

## Where we are

**Phase 3 (Lead capture)** — code-complete and closed:
- Gideon plan-check APPROVE round 2 (2026-04-17 23:40 AEST)
- Gideon phase-verify PASS round 2 (2026-04-18 02:56 AEST)
- 7 atomic commits on main, all pushed clean
- STATE.md + HANDOFF.json reflect P3 done, P4 ready

**Phase 3 UAT** — started via `/gsd-verify-work 3`, paused at 1/20:
- `.planning/3-UAT.md` created with 20 tests derived from CONTEXT + PLAN + PHASE
- Test 1 (Cold Start Smoke Test) PASSED — Sam confirmed dev server + landing page load
- Test 2 (Modal opens from CTA) PAUSED — Sam not in front of browser at ~03:00 AEST

## Pause reason

Manual UAT flow (Claude asks, Sam clicks + confirms) doesn't work when Sam isn't at a browser. Sam asked to save progress rather than pick one of the 3 resume options.

## Critical Anti-Patterns (carried forward — still applicable)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Building for cut scope | Applies every phase: verify the v1 PRD module consumes the feature before building it. | blocking | Before any "build X" task, confirm the USER + PRD module that consumes it. If no v1 module uses the feature, scope to infrastructure-only OR defer. |
| Grep gate without extension filter | Any cross-package grep MUST scope by file extension (`--include="*.ts" --include="*.tsx"`) or it drowns in node_modules. | advisory | `Grep` tool calls should include `type` or `glob` parameter. |
| Fact-bundle-only review misses wiring bugs | Specter (stateless) passes items Gideon (file-access) catches — e.g. the P0.5 /api/leads wiring bug. Single-seat Gideon on gpt-5.4 full now replaces dual-seat per Sam 2026-04-17; cost is one-seat audit, not two. | advisory | Review protocol locked to Gideon gpt-5.4 full; no Specter/Neo second seat unless explicit request. |
| Python urllib blocked by Cloudflare on Supabase Management API | Python default UA gets Cloudflare 1010. Use `curl -H "User-Agent: atlas-ai-koda/1.0"`. | advisory | Prefer curl for Supabase Management API calls. |
| PRD region deviation nearly missed | Sam provisioned Supabase in ap-southeast-1 not ap-southeast-2 per PRD §6. APP 8 disclosure landed in P3 LeadModal per DEV-001. | blocking | Any external-service provisioning step must verify deployment region against PRD §6 before writing ANY data to the service. |
| Skipping the official /gsd flow | Session 4 executed P3 via manual wave writes + direct Gideon dispatches instead of /gsd-execute-phase + /gsd-verify-work. Code correct but artifact trail bespoke. Sam flagged 2026-04-18. | blocking | For any multi-phase GSD project, follow the koda-plan default loop: brainstorming → discuss-phase → plan-phase → approval → execute-phase → verify-work. Don't hand-execute waves; don't dispatch reviewers directly — spawn the stock subagents. |

## Resume paths (Sam picks on resume)

1. **Automated UAT via Playwright** *(recommended)*
   - Load `mcp__plugin_playwright_playwright__*` tools
   - Start dev server (`cd ~/Desktop/atlas-ai/web && pnpm dev &`)
   - Drive tests 2–20 programmatically: navigate, fill, screenshot each step
   - Record pass/fail in 3-UAT.md as each test completes
   - Tests 13–14 need `.env.local` Supabase + Resend creds — skip to P4.5 gate if missing
   - Test 20 (S6 canonical-hash drift) is pure curl — easiest, do first
   - ~15 min total

2. **Manual UAT** (Sam in front of browser)
   - Sam runs `pnpm dev`, opens localhost:3000
   - Koda presents one test at a time from `.planning/3-UAT.md` §Tests
   - Sam replies yes/no per test
   - ~20 min

3. **Skip remaining UAT, generate VERIFICATION.md from Gideon PASS + code inspection**
   - Synthesize stock VERIFICATION.md from Gideon's verdict + file:line trail
   - No new runtime evidence beyond test 1
   - ~5 min

## Resume instructions

```bash
cd ~/Desktop/atlas-ai
cat .planning/.continue-here.md          # this file
cat .planning/HANDOFF.json | head -30    # P3 done, P4 entry state
cat .planning/3-UAT.md | head -40        # test 1 PASS, 2-20 pending

# Option 1 — Playwright UAT
cd web && pnpm dev &
# then in Claude: "resume UAT via playwright"

# Option 2 — Manual UAT
cd web && pnpm dev
# open http://localhost:3000
# then in Claude: "resume UAT manual, test 2"

# Option 3 — Skip remaining UAT
# in Claude: "write VERIFICATION.md from Gideon PASS"
```

<current_state>
Session 4 shipped P3 end-to-end in ~4h wall clock:
- Plan-lock: CONTEXT + PLAN + PRD v1.1 (N8N X.1.1 dormant +$200 add-on)
- Wave A: shared foundations (lead-schema, lead-score, match-stub, Field)
- Wave B: 5 step subcomponents (Step1Personal → Step5Contact)
- Wave C: LeadModal 5-step wizard + localStorage draft (Sam implemented loadDraft)
- Wave D: /api/leads atomic INSERT + scoring + dual-recipient email
- Phase-verify fixes: S6 ingress drift + MARA visa-history copy regression
- Close commit: PHASE ticked + STATE + HANDOFF

Review protocol flipped this session: single-seat Gideon on `gpt-5.4` full (supersedes dual-seat used in P0–P2). Memory: `feedback_agent-model-calibration.md` updated.

New memory this session: `feedback_atlas-ai-hand-coded.md` — atlas-ai web/ is hand-coded Next.js 16; never propose Lovable/Bolt rebuild despite PRD §5.

Uncommitted at pause: `.planning/3-UAT.md` + `.planning/.continue-here.md` (this file). Both will land in the WIP commit.
</current_state>

<completed_work>
- 7 commits on atlas-ai main this session:
  - `9a2bfd4` chore(phase-3): close P3 — Gideon PASS round 2 + governance sync
  - `68a9a9b` fix(phase-3): absorb Gideon phase-verify findings — S6 + MARA copy
  - `b210d69` feat(phase-3): /api/leads atomic INSERT + scoring + dual-recipient email
  - `3a82436` feat(phase-3): LeadModal 5-step wizard + localStorage draft hydration
  - `818fafa` feat(phase-3): per-step lead components (Step1-Step5 + APP 5/8 notice)
  - `e93202a` feat(phase-3): shared lead schema + scoring + match-stub foundations
  - `8c01d3d` chore(phase-3): plan lock — CONTEXT + PLAN + PRD v1.1 (N8N X.1.1)
- PHASE.md §Phase 3 — all 10 tasks ticked with code citations
- STATE.md snapshot — P3 → done, next phase P4 ready
- HANDOFF.json — flipped to P4 entry state
- APPROVAL.md — Sam's P3 proceed timestamp recorded (2026-04-17 23:41 AEST)
- UAT test 1 (Cold Start Smoke Test) PASSED
</completed_work>

<remaining_work>
- UAT tests 2–20 (19 tests pending)
  - Manual or Playwright-driven
  - Two tests (13 DB row + 14 email) require `.env.local` credentials
  - Test 20 proves S6 canonical-hash pinning via curl — run first
- VERIFICATION.md synthesis — stock /gsd format from Gideon's verdict + UAT results
- P4 discuss-phase (next phase) — UniMatch engine replaces client-side match-stub.ts with real /api/match
</remaining_work>

<open_questions>
- Which UAT resume path does Sam prefer? (Playwright vs manual vs skip)
- Does Sam want P4 started this session or a fresh session?
- Should governance docs (claudeking.cloud/CLAUDE.md + atlas-ai/CLAUDE.md) be updated to formally retire the dual-seat language, or leave as historical context?
</open_questions>

## P4 entry state (for when P3 UAT closes)

HANDOFF.json already flipped to P4 ready. Next phase:
- Port `web/src/lib/match-stub.ts` → real `/api/match` route
- pgvector similarity against `embeddings` table (schema ready in migrations/001:107)
- 24h per-user result cache
- Review protocol stays: single-seat Gideon gpt-5.4 full

## Key references

- `.planning/3-CONTEXT.md` — 10 P3 decisions
- `.planning/3-PLAN.md` — 8 waves + acceptance matrix
- `.planning/STATE.md` — phase snapshot
- `.planning/HANDOFF.json` — structured state (P3 done → P4 ready)
- `.planning/3-UAT.md` — 20 tests, 1/20 passed
- `planning/atlas-ai/APPROVAL.md` — Sam's approval trail
- `/tmp/atlas-p3-phaseverify-r2-output.md` — Gideon PASS (will be VERIFICATION.md input)

## Review protocol reminder

Single-seat Gideon on `codex exec -m gpt-5.4 --full-auto` (full, not mini).
No Neo. No Specter. No Atlas. See `feedback_agent-model-calibration.md`.
