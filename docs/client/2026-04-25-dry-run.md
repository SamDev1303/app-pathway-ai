# Dry Run — 2026-04-25 (pre-meeting rehearsal)

**Run this 5–10 min before the 15:30 AEST call.** Click each step on prod. Each line has the expected outcome — if any line is RED, fix or skip in the live demo.

Prod URL: https://atlas-ai.vercel.app (or current alias)

---

## 1. Landing page loads clean (30s)
- [ ] Open prod URL in a fresh incognito window
- [ ] Hero renders, no console errors (F12 → Console — should be empty/yellow only)
- [ ] MARA / QEAC / CRICOS attestation badges visible
- [ ] Footer + Liverpool CTA visible

**If broken:** screenshare a different surface, or skip to chat demo.

---

## 2. Matcher widget — happy path (45s)
- [ ] Click "Find my unis" / matcher CTA
- [ ] Step through all 5 lead-capture screens with realistic fake data:
  - Name: `Test Demo`  · Email: `demo+test@claudeking.org`  · Phone: any
  - Country: India · Field: Business · Budget: 30k-50k AUD · Start: 2026 intake
- [ ] On final submit: redirects to `/matches/[token]` page
- [ ] Page shows ≥3 matched unis with score + "Why this match" reasoning

**Expected:** redirect to /matches/[token], 3-5 ranked unis. **If 0 matches:** RPC `match_unis_for_lead` is broken — pivot to chat demo.

---

## 3. AI Advisor Chat — RAG grounding (60s)
- [ ] Open chat widget (bottom-right)
- [ ] Type: `What courses can I do at UWA for business analytics?`
- [ ] Wait for stream — should complete in <8s
- [ ] Response cites specific UWA course names (e.g., "Master of Business Analytics")
- [ ] `SOURCES (5) ▼` pill visible under the answer — click to expand, 5 course cards show

**Expected:** 5 cited courses, no "I don't have that information". **If `noHits:true`:** embeddings backfill regressed — check Supabase `course_embeddings` row count (should be 68).

---

## 4. Chat compliance deflection (30s)
- [ ] In same chat, type: `Will you guarantee my visa approval?`
- [ ] Response should:
  - NOT promise approval
  - Mention MARA / migration agent referral
  - Stream cleanly (no error toast)

**Expected:** polite deflection with MARA disclaimer. **If it promises:** layer-2/3 deflection broken — DO NOT live-demo this prompt.

---

## 5. SOP endpoint sanity (15s — terminal, not browser)
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://atlas-ai.vercel.app/api/sop \
  -H "Content-Type: application/json" -d '{"leadId":"not-a-uuid"}'
```
- [ ] Returns `400` (zod UUID validation)

**Expected:** `400`. If `500` → server error, skip SOP mention in demo. If `200` → validation regressed.

---

## 6. Lead notification email (optional, 30s)
- [ ] Check inbox at `sam@claudeking.org` for the lead from step 2
- [ ] Email arrives within 60s of submit, contains lead details + match link

**If no email by 14:25:** `LEAD_NOTIFY_EMAILS` env var didn't propagate; mention "email notify is in flight" if asked.

---

## Pre-flight checklist (do these BEFORE step 1)
- [ ] Laptop charging, charger plugged in
- [ ] Browser zoom at 100% (Cmd+0)
- [ ] Slack/Telegram/email notifications muted (Cmd+Shift+D or Do Not Disturb)
- [ ] Second monitor (if any) shows `2026-04-25-screenshare-notes.md` for Q&A reference
- [ ] Demo script open in another tab: `2026-04-25-demo-script.md`
- [ ] Test the Zoom/Meet link 2 min before — share screen test

---

## Abort criteria (when to NOT live-demo)
- Step 1 fails → site down, switch to "I'll walk you through screenshots"
- Step 3 fails with `noHits:true` → chat is broken silently, skip Q3 entirely
- Step 4 promises visa approval → compliance liability, do not show

If any of those: send the client `2026-04-25-status-message.md` framing a 24h delay, then debug.

---

## Time budget
- Whole dry run: 5 min if green, 10 min if you debug one step
- Stop debugging at 15:20 — you need 10 min buffer before 15:30 call
