# Review digest — org-loop 20260924-234253-code-wt-agents-md-only-fdb6f30-41303
<!-- Written by agents/scripts/board-merge.py digest. Regenerated in place each round — never appended to. -->

- repo: `/Users/shamalkrishna/Work/Pathway-AI/code-wt-agents-md-only`
- base: `origin/main` · head: `6cd282261f73`
- score: **4/5** (minor polish) · open 3 · not counted 6
- score rule that fired: 3 unresolved P2/P3 only

## Seats
| model | group | blind | cross-exam |
|---|---|---|---|
| z-ai/glm-5.3-flash | glm | FLAG | FLAG |
| deepseek/deepseek-v4.1-flash | deepseek | FLAG | FLAG |

## Findings
One line per finding: id · sev · status · file:line · claim

- F1 · P2 · out-of-scope · `AGENTS.md (new file, header line)` · Agent roster omits Atlas: header says "Rules every agent (Claude, Gideon, Neo, minis)" while the same file's workflow block names "Antigravity/Atlas" as a first-class seat — an Atlas agent has no rule coverage claimed for it
- F2 · P2 · out-of-scope · `AGENTS.md (new file, approval-gate bullet)` · The new approval-gate bullet admits `.planning/config.json` `approval_gate` still names the deleted `planning/pathway-ai/APPROVAL.md` but the diff contains no fix to that config — the stale pointer survives the very commit that documents it as stale
- F3 · P2 · ungrounded · `AGENTS.md:152` · §9 says v1 chat runs through OpenRouter while the locked-stack bullet three lines above still names OpenAI as the provider, so an agent reading §9 cannot tell which provider is locked.
- F4 · P2 · suppressed · `AGENTS.md:213` · The workflow block states "Only Astra (the builder lane) and Koda build or merge; every other seat reviews, researches or drafts", but §7 (AGENTS.md:125) assigns Gideon to write TS APIs and SQL migrations — the one rules file gives contradictory write authority.
- F5 · P2 · out-of-scope · `AGENTS.md (new file, header line)` · (cross-exam vote, not a new finding)
- F6 · P2 · open · `README.md:54` · The touched governance line still says "Specter/NeMo Tron as fallback", but the new AGENTS.md §4 renames the fallback to "Specter (lane `mini-specter`, Nemotron)" — the off-plan register claims stale seat model ids were corrected, yet the renamed terminology survives in lines this commit edited
- F7 · P2 · unconfirmed · `PHASE.md:5` · Same drift in the touched sign-off-rule line: "NeMo Tron fallback" naming kept while AGENTS.md §4 now defines the fallback as Specter/`mini-specter`
- F8 · P2 · open · `AGENTS.md:60 (§3a check)` · The route-count glob `web/src/app/api/*/route.ts` only matches one directory level, so a nested route (e.g. `web/src/app/api/leads/[id]/route.ts`) is invisible to `routes` while its row still counts in `rows`, producing a false MISMATCH on every API commit
- F9 · P2 · suppressed · `AGENTS.md:51` · The new §3a route-table guard silently passes when run outside the repo root — and this same file's checks section tells the author to `cd web` first — because the glob and the awk both degrade to `0`, so `[ "0" = "0" ]` is true and nothing is printed.

## Rounds
- r1 · head fdb6f3065b99 · 4/5 · new 0 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash
- r2 · head 5a03f4c8163b · 5/5 · new 0 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash
- r3 · head 6cd282261f73 · 4/5 · new 3 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash

Board: `/Users/shamalkrishna/.cache/koda/org-loops/20260924-234253-code-wt-agents-md-only-fdb6f30-41303/BOARD.md` (machine-level state — this digest outlives it).
