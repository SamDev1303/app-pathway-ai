---
context: phase_boundary
phase: P3 (next)
task: 0
total_tasks: 9
status: paused_at_phase_boundary
last_updated: 2026-04-17T12:43:22.151Z
---

# Atlas AI — Paused between P2 (done) and P3 (next)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Building for cut scope | P2 was initially drafted as "build magic-link for admin dashboard" — but X.1 admin dashboard is cut to v2. Koda push-back → Sam confirmed infrastructure-only. | blocking | Before any "build feature X" task, confirm the USER + PRD module that consumes it. If no v1 module uses the feature, scope to infrastructure-only OR defer. |
| Grep gate without extension filter | Initial P0.5 grep returned 659 hits dominated by `node_modules`. `--include="*.ts" --include="*.tsx"` cut noise to real 12 files. | advisory | Any cross-package grep MUST scope by file extension. |
| Fact-bundle-only review misses wiring bugs | Specter passed P0.5 item 6 from summary; Gideon (file-access) caught /api/leads schema mismatch. | advisory | Both seats needed on schema-wiring tasks — one with live code access, one stateless adjudicator. |
| Python urllib blocked by Cloudflare on Supabase Management API | Python's default User-Agent gets Cloudflare 1010 block. Curl with explicit UA works. | advisory | Always use `curl -H "User-Agent: atlas-ai-koda/1.0"` for Supabase Management API calls. |
| PRD region deviation nearly missed | Sam provisioned Supabase in ap-southeast-1 (Singapore) not PRD §6's ap-southeast-2 (Sydney). Koda's Management API region check caught it BEFORE schema applied. | blocking | Any external-service provisioning step must verify deployment region against PRD §6 before writing ANY data to the service. |

<current_state>
Session 3 shipped 3 phases end-to-end with dual-seat Gideon + Specter review on each. P0.5 (MARA scrub), P1 (Supabase schema + seed), P2 (auth infrastructure) are CLOSED with sign-offs recorded in PHASE.md. All 10 commits pushed clean to `SamDev1303/unimate-demo.git`.

Current repo state: clean, no uncommitted files. Supabase project `szuqcptsmmgycvagteza` is live (12 unis + 48 courses, RLS verified) in `ap-southeast-1` per DEV-001 deviation. Auth infrastructure wired (login page + callback + middleware + server/client helpers).

Paused at the phase boundary before P3 Lead capture begins. P3 is the revenue module — 5-step progressive form for lead capture with consent-gated atomic write. Most DB + API infrastructure is ready (`/api/leads/route.ts` already accepts the split consent shape from P0.5 commit 4a81cc8; RLS policies allow anon INSERT with `consent_service=true`; Resend email wiring already present).
</current_state>

<completed_work>

Completed this session:
- **P0.5** (MARA scrub) — 14 files scrubbed across 3 rounds, 3 commits (f425837, 2bedf83, 4a81cc8), Gideon caught legitimate issues in rounds 1 + phase-verify item 6. Dual-seat PASS.
- **P1** (Supabase schema + seed) — schema applied via Management API, 12 unis + 48 courses seeded, RLS verified live. Region deviation ap-southeast-1 vs ap-southeast-2 documented as DEV-001. 3 commits (32ad7bb, 5ac9bf7, 911379b). Dual-seat PASS.
- **P2** (Auth infrastructure) — `@supabase/ssr` wired, middleware/callback/login page shipped, Supabase Auth config applied. Shipped as INFRASTRUCTURE ONLY per Koda push-back + Sam approval (admin dashboard X.1 cut to v2). 2 commits (809d7dc, 93f9631). Dual-seat PASS.

All 17 session-start tasks (ids 1-17) completed; P2 sub-tasks 26-32 completed.
</completed_work>

<remaining_work>

## P3 — Lead Capture (ordered tasks)

1. Neo research — Privacy Act 1988 s.6 + APP 3/5 consent wording; service-vs-marketing split patterns
2. Step 1 shows explicit APP 5 collection notice BEFORE PII entry
3. UX copy surfaces localStorage trade-off (device-only drafts, clear-browser-data loses progress)
4. Steps 1-4 persist to `localStorage` client-side only (ZERO pre-consent server writes)
5. Step 5 atomic INSERT into `leads` with form fields + `consent_given_at` + `consent_wording_version` + `consent_service` (required true) + `consent_marketing` (optional)
6. Required service consent checkbox + optional marketing checkbox; submit blocked until service ticked
7. Submission emails Sam + UniMate via Resend (routing already wired in `/api/leads/route.ts`)
8. Lead score computed server-side in the same atomic write
9. DB audit shows zero rows with `consent_service=false`

Plus dual-seat plan-check + phase-verify (Gideon + Specter) + commit + push.
</remaining_work>

<decisions_made>

## Session 3 decisions (carried forward)

- **P2 scope = infrastructure only** — no admin UI, no middleware redirect. Rationale: PRD §4 lists auth in stack but V1.1-V1.4 are anonymous; admin dashboard X.1 cut to v2.
- **Region ap-southeast-1 accepted** as DEV-001 deviation — Sam override of PRD §6 after Koda push-back; 4 APP 8 obligations captured in `planning/atlas-ai/DEVIATIONS.md` for P4.5 compliance gate.
- **12 unis seeded (not 43)** — seed file reality; P4.5 backfill task documented.
- **wants_pr → prioritize_outcomes** + **pr_eligible → industry_placement** — MARA rule: app must not score on migration-relevant criteria.
- **Specter over Neo as dual-seat 2** — Neo OpenCode `run --mode=plan` flag invalid; Specter NIM Nemotron-3-Super-120B reliably fills the seat.
</decisions_made>

<blockers>
None for starting P3. All infrastructure dependencies satisfied.

## P4.5 production gates carrying forward (DO NOT mark P5 done until addressed)
1. APP 8 Singapore disclosure in LeadModal notice (P1 DEV-001)
2. 12→43 unis backfill (P1 scope gap)
3. Per-course CRICOS codes (all 48 NULL) (P1 known limitation)
4. `industry_placement` DB column (P0.5 rename)
5. Middleware `auth.getUser()` → `auth.getClaims()` migration (P2 reviewer note)
</blockers>

## Required Reading (in order — before acting on P3)

1. `PHASE.md` §P3 — canonical task list (don't re-discover from memory)
2. `PRD.md` §V1.2 + §5 (Architecture) + §6 (AU compliance) — consent wording requirements
3. `planning/atlas-ai/DEVIATIONS.md` DEV-001 — Singapore region obligation: LeadModal APP 5 notice MUST include Singapore hosting disclosure per APP 8
4. `web/src/components/LeadModal.tsx` — current 1-step modal (needs 5-step rework)
5. `web/src/app/api/leads/route.ts` — server route already wired to new consent shape; don't re-architect, just call from the 5-step form
6. `web/src/lib/types.ts` — `LeadInput` interface (consent_service, consent_marketing, consent_wording_version)
7. `supabase/migrations/001_initial_schema.sql` — leads table columns + CHECK constraints
8. `.planning/research/p0.5-plan-check/gideon-v2.md` — what Gideon expected LeadModal consent split to look like
9. `.planning/METHODOLOGY.md` (if exists) — project analytical lenses

## Critical Anti-Patterns (do NOT repeat)

- **Don't forget APP 8 Singapore disclosure in P3 LeadModal** — DEV-001 explicitly says this MUST be added before real leads land. This is the #1 production gate from DEV-001.
- **Don't replace localStorage persistence with server-side step saves** — PRD V1.2 specifies ZERO pre-consent PII on server. Steps 1-4 are client-only.
- **Don't bypass `consent_service=true` CHECK constraint** — it's a Postgres-level enforcement; bypass attempts throw 42501. Submit MUST enforce the checkbox.
- **Don't hardcode Supabase secrets in any written file** — use `SUPABASE_ACCESS_TOKEN` env var reference. The pre-commit secret scan hook will block commits with `sbp_...` literals.

## Infrastructure State

- **Supabase project:** `szuqcptsmmgycvagteza` (ap-southeast-1) — ACTIVE_HEALTHY
- **Tables seeded:** universities (12), courses (48), leads (0), embeddings (0)
- **Auth config applied:** site_url, uri_allow_list, 1h OTP expiry, external_email_enabled
- **Secrets saved locally:** `web/.env.local` (gitignored via `.env*`) — anon JWT, service_role JWT, publishable, secret, access_token PAT, DB password
- **RLS posture verified:** anon SELECT universities/courses OK; anon SELECT leads → `[]` (default-deny); anon INSERT leads with consent_service=true → 201 OK; service_role bypasses
- **CLI:** Supabase CLI logged in to a DIFFERENT account than the atlas-ai project owner. Use Management API (with PAT) for ops, OR Sam needs to `supabase login` on the right account before `supabase db push` would work.

<context>
Mental state at pause: Session pushed through 3 consecutive phase closures with dual-seat review on each (rigorous but context-expensive). Everything is tracked in PHASE.md + DEVIATIONS.md + .planning/research/ artifacts. No silent deferrals. P3 is clean to start — API + DB + consent shape + Resend already wired from P0.5 work, so P3 should mostly be front-end 5-step form + server-side lead scoring + phase review.

The "3 consecutive phases" pattern was driven by Sam's velocity preference ("continue", "finish gsd phases", "do it"). Worked well this session but CLAUDE.md's recommended `/gsd:pause-work` → `/clear` → `/gsd:resume-work` pattern exists for a reason — next session should start fresh to avoid context-rot bugs sneaking into the $3k revenue module.
</context>

<next_action>
Start with: `/gsd-resume-work` from ~/Desktop/atlas-ai — then `/gsd-discuss-phase 3` to lock the Lovable-vs-hand-coded decision for the 5-step form, then `/gsd-plan-phase 3`.

If jumping straight in (skipping discuss): start by reading `web/src/components/LeadModal.tsx` + `web/src/app/api/leads/route.ts` + `planning/atlas-ai/DEVIATIONS.md` DEV-001. Then design the 5-step state machine + localStorage schema. Then convert LeadModal. Then add server-side lead scoring in /api/leads. Then dispatch plan-check + phase-verify.
</next_action>
