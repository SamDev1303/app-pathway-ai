# 4.5-CONTEXT.md — Phase 4.5 Compliance Gate (Pathway-AI)

**Phase:** 4.5 — Compliance gate (HARD BLOCK on P5)
**Session:** 2026-04-20 01:45 AEST Monday → target close 2026-04-20 ~04:30 AEST
**Owner:** Koda (main thread) · Gideon (plan-check + phase-verify, single-seat gpt-5.4)
**Approval:** `planning/pathway-ai/APPROVAL.md` — pending this session's commit
**Why this phase exists:** Gideon P0 plan-check — compliance cannot sit at P8 while P5 is the live chat surface. P4.5 verifies / Privacy Act 1988 / APP compliance BEFORE AI chat launches, not after.

---

## Locked decisions (D1–D10)

### D1 — registration number handling
**Decision (revised mid-session 02:05 AEST):** All 5 production render paths (login, LeadModal, MatcherForm, Step1Personal, Step5Contact) + Footer + content.ts use `[PENDING_FROM_UNIMATE]` square-bracket placeholder for / QEAC / ABN. CI grep gate fails on `PENDING_FROM_UNIMATE` after a 7-day grace window. **Square brackets (not curly) are required because JSX parser treats `{...}` as an expression — scrub failed initial typecheck until converted.**
**Rationale:** Pathway-AI hasn't delivered the yet; `` + `QEAC P538` + `ABN 12 345 678 901` were scaffolded as narrative fillers during P0.5/P3 but looked like real regulatory identifiers. Playwright-verified via portal.mara.gov.au on 2026-04-20 02:02 AEST: both and business "Pathway-AI" return "no records to display". This was a Privacy Act 1988 APP 5 + Act 1958 s.280 liability surface.
**Evidence:** `.planning/research/p4.5-mara-registry-verify/findings.md` + 2 screenshots.
**Owner:** Sam must request number from Pathway-AI before 2026-04-27 23:59 AEST.
**Enforcement:** `.github/workflows/mara-grep-gate.yml` — step "mara-number-placeholder" fails if `PENDING_FROM_UNIMATE` appears in `web/src/**` and the grace window has expired.

### D2 — Supabase data residency
**Decision:** Defer the ap-southeast-1 (Singapore) vs ap-southeast-2 (Sydney) decision to P8 final audit. Document as OPEN ITEM / P8 blocker in `docs/compliance-attestation-v1.md`. Do NOT migrate data in P4.5.
**Rationale:** Sam override — zero prod traffic on the DB today; defer cost is acceptable; P8 can decide informed by final client handover context.
**Risk flag:** P5 chat will launch on Singapore infra before this decision lands. APP 8 cross-border disclosure is already in Step5Contact consent text (CONSENT_WORDING_VERSION = 2026-04-17.v2), so legal coverage holds. Attestation doc must explicitly call out this open item.
**Side-effect:** PHASE.md line 205 "`ap-southeast-2`" is factually wrong — edit to `ap-southeast-1 per DEV-001 (decision deferred to P8 per 4.5-CONTEXT.md D2)`. Similarly 001_initial_schema.sql line 2 comment gets corrected.

### D3 — Chat system prompt scope for P4.5
**Decision:** Staging constant only. Ship `web/src/lib/chat-system-prompt.ts` as an exported `CHAT_SYSTEM_PROMPT_V1` constant. No route wiring, no provider call, no feature flag. P5 owns `/api/chat` end-to-end.
**Rationale:** P4.5 scope is compliance verification, not AI plumbing. Staging the prompt gives P5 a known-good starting point reviewed under lens now, before P5 pressure forces compromise.

### D4 — RLS policy model
**Decision:** `leads` table — anon role has INSERT only, service role has full SELECT/UPDATE/DELETE. `universities` + `courses` — anon role has SELECT only (they're public reference data). `lead_matches` (if exists) — service role only.
**Rationale:** Privacy Act 1988 APP 11 (security of personal information) requires lead data to be unreadable by anon. Universities/courses are public reference — anon needs read to render UI.
**Implementation:** `supabase/migrations/006_rls_policies.sql` creates policies declaratively. Wave 3 applies via Supabase Management API (HITL gated before push).

### D5 — Scope of carry-forward items from earlier phases
**Decision:** P4.5 only addresses the 9 PHASE.md tasks (lines 198-207) + 2 Gideon P4 phase-verify deferrals (REVOKE/GRANT harness partial, browser UAT skip). Does NOT:
- Backfill 43 unis or cricos_code on courses (deferred to P8 — data task, not compliance)
- Migrate `auth.getClaims()` from legacy `auth.getUser()` (P2 carry-forward, non-blocking code quality)
- Solve QS WUR licensing lawyer-review flag (document-only, defer to P8)
- Do 31-min browser UAT for /matches/[token] expiry (deferred — not a compliance gate; move to P8 UAT suite)

### D6 — Footer site-wide wiring
**Decision:** Move `<Footer />` render from `app/page.tsx` into `app/layout.tsx` so it renders on every route. The home page currently renders it inline; remove that render to avoid double-render.
**Rationale:** disclosure is a regulatory requirement on every surface, not just the home page. Moving to layout is the right place.
**Side-effect:** `app/layout.tsx` body structure changes — main content area wraps in a `<main>` with `flex-1` so footer sinks to bottom.

### D7 — Consent UI verification (not rebuild)
**Decision:** The split `consent_service` + `consent_marketing` schema is LIVE (P3). Step5Contact renders both checkboxes with APP 5 + APP 8 disclosure text already. P4.5 audits this visually and via screenshot — does NOT rebuild. If audit finds a gap, we fix; otherwise no code change.

### D8 — Compliance attestation deliverable
**Decision:** `docs/compliance-attestation-v1.md` is the single client-facing compliance artifact produced by P4.5. Structure:
1. compliance (disclaimer + registration # placement + CI gate)
2. Privacy Act 1988 (consent split + wording version + APP 8 cross-border notice)
3. APP 11 Security (RLS policies verified + AES-256 at rest + TLS 1.2+ in transit)
4. APP 5 notice placement (Step5Contact rendering verified)
5. Open items carried to P8 (region decision, # replacement, QS WUR licensing)

### D9 — CI grep gate scope
**Decision:** `.github/workflows/mara-grep-gate.yml` runs on PR + push to main. Checks two things:
- **Forbidden strings:** `visa`, `migration`, `\bPR\b`, `MLTSSL`, `subclass`, `points test`, `post-study work` — fails if found in `web/src/**` except in `mara-disclaimer.ts` + `chat-system-prompt.ts` (allowlisted by path).
- **Placeholder enforcement:** fails if `PENDING_FROM_UNIMATE` appears on main after 2026-04-27.

### D10 — Review protocol (P4.5)
**Decision:** Single-seat Gideon gpt-5.4 for both plan-check and phase-verify (same as P3 + P4). No Neo / no Specter / no Atlas. Per `feedback_agent-model-calibration.md` + approved protocol.
**Iteration cap:** 3 rounds of plan-check; if Gideon BLOCKS on round 3, escalate to Sam decision.

---

## Out of scope (explicit exclusions)

- Any change to `web/src/app/api/chat/route.ts` — P5 territory
- Any AI SDK or provider install — P5 territory
- Mobile app / Expo — P7 territory
- SOP generator / react-pdf — P6 territory
- Production deploy — P9 territory
- 43-uni data backfill — not compliance, P8 data task

---

## Expected artifacts (end of P4.5)

### New files (6)
- `web/src/lib/chat-system-prompt.ts` — staging constant for P5
- `supabase/migrations/006_rls_policies.sql` — RLS lockdown
- `.github/workflows/mara-grep-gate.yml` — CI enforcement
- `docs/compliance-attestation-v1.md` — client-facing compliance doc
- `planning/pathway-ai/4.5-CONTEXT.md` — this file
- `planning/pathway-ai/4.5-PLAN.md` — executable plan

### Modified files (4)
- `web/src/lib/mara-disclaimer.ts` — add registration # reference + link
- `web/src/app/layout.tsx` — render `<Footer />` site-wide
- `web/src/app/page.tsx` — remove inline `<Footer />` (now layout-owned)
- `PHASE.md` — P4.5 sign-off + line 205 correction + line 201-206 checkboxes

### No changes (verified only)
- `web/src/components/Footer.tsx` — already has slot, just content.ts placeholder
- `web/src/components/lead/Step5Contact.tsx` — already renders both consent checkboxes + APP 8 disclosure

---

## Assumptions + pre-reqs

- Supabase project `szuqcptsmmgycvagteza` ap-southeast-1 Singapore is live + reachable
- Supabase Management API PAT scope is correct (per P4 Gideon deferral — if not, Wave 3 blocks)
- Sam approves HITL pause at Wave 3 before live DB push
- `gh` CLI authed for repo `SamDev1303/unimate-demo`
- No concurrent session modifying `web/src/**` or `supabase/migrations/**` during this run
