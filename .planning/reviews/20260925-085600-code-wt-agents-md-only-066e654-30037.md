# Review digest — org-loop 20260925-085600-code-wt-agents-md-only-066e654-30037
<!-- Written by agents/scripts/board-merge.py digest. Regenerated in place each round — never appended to. -->

- repo: `/Users/shamalkrishna/Work/Pathway-AI/code-wt-agents-md-only`
- base: `origin/main` · head: `cdbac74568ed`
- score: **4/5** (minor polish) · open 1 · not counted 5
- score rule that fired: 1 unresolved P2/P3 only

## Seats
| model | group | blind | cross-exam |
|---|---|---|---|
| z-ai/glm-5.3-flash | glm | FLAG | FLAG |
| deepseek/deepseek-v4.1-flash | deepseek | FLAG | FLAG |

## Findings
One line per finding: id · sev · status · file:line · claim

- F1 · P2 · ungrounded · `AGENTS.md:5` · The header asserts `web/AGENTS.md` exists and "adds the Next.js version notes", but the diff only deletes `web/CLAUDE.md` and never shows or creates `web/AGENTS.md` — if it is absent, `web/` agents silently lose all governance after the import file is removed
- F2 · P2 · ungrounded · `SOURCECODE.md:226` · The same invariant is stated two ways: this line defines the count as `app/api/*/route.ts` (one level), while AGENTS.md §3a counts with `find web/src/app/api -name route.ts` (recursive, "nested routes count too"); once any nested route exists, an author who trusts this line sees "4 = 4 ✅" while the AGENTS.md check prints MISMATCH, so the two gates disagree about the same table.
- F3 · P2 · suppressed · `.planning/config.json:25` · `claude_md_path` is a second pointer at the same file already named by `"governance": "AGENTS.md"` (line 40), written in a different form; if GSD resolves it against the config's own directory (`.planning/`) rather than the repo root — as the neighbouring `"state": ".planning/STATE.md"` suggests is possible — it targets a nonexistent `.planning/AGENTS.md`, and the two keys can drift apart with neither noticing.
- F4 · P2 · open · `scripts/check-route-table.sh:9` · The script only compares total counts, but its header and AGENTS.md §3a claim "every route.ts has one `/api/` row" — a duplicate row for one route plus a missing row for another passes as `ok: 4 = 4`, so the drift the check exists to catch can still land silently
- F5 · P2 · suppressed · `SOURCECODE.md:355` · Says the route-table check "will fail CI", but AGENTS.md §3a only instructs each author to run `bash scripts/check-route-table.sh` by hand and this change wires the new script into no CI job, so a skipped manual run ships a stale table with nothing complaining.
- F6 · P2 · suppressed · `scripts/check-route-table.sh:10` · The gate compares only totals, so renaming an existing route (`/api/match` → `/api/unimatch`) leaves 4 files = 4 rows and prints "ok" while SOURCECODE.md still advertises the dead path — exactly the doc drift §3a exists to prevent.

## Rounds
- r1 · head 066e6549fb4e · 4/5 · new 0 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash
- r2 · head 803bbce4b960 · 4/5 · new 0 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash
- r3 · head cdbac74568ed · 4/5 · new 1 · closes 0 · seats z-ai/glm-5.3-flash,deepseek/deepseek-v4.1-flash

Board: `/Users/shamalkrishna/.cache/koda/org-loops/20260925-085600-code-wt-agents-md-only-066e654-30037/BOARD.md` (machine-level state — this digest outlives it).
